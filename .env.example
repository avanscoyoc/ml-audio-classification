# EXAMPLE Environment file for ML Audio Classification
# GCP Settings matching CLAUDE.md specifications

# Google Cloud Platform Settings (REQUIRED)
GCP_PROJECT_ID=name-of-your-gcp-project
GCS_BUCKET_NAME=name-of-your-gcs-bucket
GOOGLE_APPLICATION_CREDENTIALS=/path/to/secret.json

# Optional: GPU support (requires nvidia-docker)
# NVIDIA_VISIBLE_DEVICES=all